# 유수언 위키 프로젝트

## 개요

이 프로젝트는 중국 애니메이션 '유수언(有兽焉)'의 팬들을 위한 정보 제공 웹사이트입니다. 현재는 정적 데이터(data.js)를 기반으로 캐릭터 정보를 보여주고 있지만, 사용자들이 직접 정보를 수정하고 기여할 수 있는 동적인 위키로 발전시키는 것을 목표로 합니다.

## 주요 기능 (현재)

*   **메인 페이지:** 프로젝트 소개 및 주요 콘텐츠(추천 에피소드, 캐릭터)로의 링크 제공
*   **애니메이션 보기:** 시즌별 에피소드 리스트 및 비디오 플레이어 기능
*   **캐릭터 목록:** 전체 캐릭터를 그리드 형태로 전시 및 검색 기능
*   **캐릭터 상세 정보:** 각 캐릭터의 이미지, 소개, 통계 등의 상세 정보 제공

## 디자인 및 스타일 가이드

*   **색상:** 밝은 테마를 기반으로 하며, 주요 색상은 유수언 공식 아트워크에서 영감을 얻습니다.
*   **폰트:** 가독성이 좋은 'Inter' 폰트를 메인으로 사용합니다.
*   **레이아웃:** 모바일 반응형 디자인을 적용하여 다양한 기기에서 최적의 경험을 제공합니다.

---

## 현재 작업 계획: 사용자 편집 기능 구현 (Firebase 연동)

**목표:** 사용자가 위키의 캐릭터 정보를 직접 편집하고 저장할 수 있도록 Firebase Firestore를 연동합니다.

**세부 단계:**

1.  **Firebase 설정:**
    *   Firebase SDK를 `index.html`에 추가합니다.
    *   Firebase 프로젝트 설정을 위한 `firebase-config.js` 파일을 생성합니다.

2.  **데이터베이스 이전:**
    *   기존 `data.js`에 있던 캐릭터, 카테고리 데이터를 Firestore 데이터베이스로 이전합니다.

3.  **데이터 로딩 방식 변경:**
    *   `main.js`와 `detail.js`가 `data.js` 대신 Firestore에서 데이터를 비동기적으로 로드하도록 수정합니다.
    *   `renderCharGrid`, `renderCharacterDetail` 등의 함수를 Firestore 데이터 구조에 맞게 조정합니다.

4.  **편집 인터페이스 추가:**
    *   `detail.html`에 '편집' 버튼을 추가합니다.
    *   편집 버튼 클릭 시, 정보 필드(이름, 소개 등)가 `<span>`이나 `<p>`에서 `<input>`이나 `<textarea>`로 전환되도록 UI를 동적으로 변경하는 기능을 `detail.js`에 구현합니다.

5.  **저장 기능 구현:**
    *   '저장' 버튼을 추가하고, 클릭 시 사용자가 수정한 내용을 Firestore 데이터베이스의 해당 캐릭터 문서(document)에 업데이트하는 로직을 `detail.js`에 추가합니다.
    *   업데이트가 완료되면 다시 읽기 전용 뷰(read-only view)로 전환합니다.
