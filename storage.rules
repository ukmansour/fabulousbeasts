rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /characters/{charId}/{allPaths=**} {
      // 누구나 이미지를 볼 수 있도록 허용
      allow read: if true;
      // 로그인한 사용자만 이미지 업로드 가능 (최대 25MB 제한은 클라이언트에서 처리 중)
      allow write: if request.auth != null;
    }
  }
}
