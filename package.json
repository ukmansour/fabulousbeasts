{
  "name": "fabulousbeasts",
  "version": "1.0.0",
  "scripts": {
    "build": "node build.js",
    "deploy": "npm run build && npx wrangler deploy"
  },
  "dependencies": {
    "firebase": "^10.8.0"
  },
  "devDependencies": {
    "wrangler": "^3.114.17"
  }
}
